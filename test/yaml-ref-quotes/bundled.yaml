openapi: 3.0.2
components:
  schemas:
    Case:
      description: Case file subset
      type: object
      properties:
        extern:
          $ref: '#/components/schemas/Extern'
        intern:
          $ref: '#/components/schemas/Extern'
    Extern:
      type: object
      properties:
        id:
          type: string